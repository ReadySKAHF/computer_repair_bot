# 🔧 Telegram Bot для сервиса ремонта компьютеров

Современный Telegram бот для приема заказов на ремонт компьютеров с ИИ консультантом, системой отзывов и удобным интерфейсом.

## ✨ Основные возможности

### 👤 Для клиентов
- 📝 **Простая регистрация** с валидацией данных
- 🛠️ **Создание заказов** с выбором услуг, времени и даты
- 🤖 **ИИ консультант** для диагностики проблем на базе Google Gemini
- 📋 **Каталог услуг** с подробными описаниями
- 👥 **Информация о мастерах** с рейтингами
- ⭐ **Система отзывов** с возможностью оценки работы
- 👤 **Управление профилем** и историей заказов
- 💬 **Техническая поддержка** с FAQ

### 🛡️ Технические особенности
- ✅ **Валидация всех пользовательских данных**
- 🗃️ **Надежная работа с базой данных** с обработкой ошибок
- 🏗️ **Модульная архитектура** для легкого расширения
- 📊 **Логирование всех операций**
- 🔒 **Безопасная обработка конфигурации**

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd computer_repair_bot

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Создайте файл `config.txt` в корневой папке:

```ini
# Обязательные параметры
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Дополнительные настройки (необязательно)
DB_PATH=repair_bot.db
LOG_LEVEL=INFO
MAX_SERVICES_PER_ORDER=10
MAX_MESSAGE_LENGTH=1000
RATE_LIMIT_MESSAGES=30
RATE_LIMIT_WINDOW=60

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321
```

### 3. Получение токенов

#### Токен Telegram бота:
1. Перейдите к [@BotFather](https://t.me/BotFather)
2. Создайте нового бота командой `/newbot`
3. Скопируйте токен в `config.txt`

#### API ключ Gemini:
1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Создайте новый API ключ
3. Скопируйте ключ в `config.txt`

### 4. Запуск бота

```bash
python -m app.main
```

## 📁 Структура проекта

```
computer_repair_bot/
├── app/
│   ├── __init__.py
│   ├── main.py                    # Точка входа
│   ├── config.py                  # Конфигурация
│   │
│   ├── handlers/                  # Обработчики сообщений
│   │   ├── registration.py        # Регистрация пользователей
│   │   ├── orders.py             # Создание заказов
│   │   ├── services.py           # Каталог услуг
│   │   ├── profile.py            # Управление профилем
│   │   ├── reviews.py            # Система отзывов
│   │   ├── ai_consultation.py    # ИИ консультации
│   │   └── support.py            # Техподдержка
│   │
│   ├── database/                  # Работа с БД
│   │   ├── connection.py         # Подключение к БД
│   │   └── queries.py            # SQL запросы
│   │
│   ├── services/                  # Бизнес-логика
│   │   ├── ai_service.py         # ИИ сервис
│   │   └── validation_service.py # Валидация данных
│   │
│   ├── keyboards/                 # Клавиатуры
│   │   ├── main_menu.py          # Главное меню
│   │   ├── order_keyboards.py    # Клавиатуры заказов
│   │   └── profile_keyboards.py  # Клавиатуры профиля
│   │
│   └── utils/                     # Утилиты
│       ├── validators.py         # Валидаторы
│       └── constants.py          # Константы
│
├── config.txt                     # Конфигурация
├── requirements.txt               # Зависимости
└── README.md                      # Документация
```

## 💾 База данных

Бот использует SQLite базу данных со следующими таблицами:

- **users** - информация о пользователях
- **services** - каталог услуг
- **masters** - информация о мастерах
- **orders** - заказы клиентов
- **order_services** - связь заказов с услугами
- **reviews** - отзывы клиентов
- **support_requests** - обращения в поддержку

База данных создается автоматически при первом запуске и заполняется тестовыми данными.

## 🤖 ИИ Консультант

Бот интегрирован с Google Gemini для предоставления умных рекомендаций:

### Возможности ИИ:
- 🔍 **Анализ проблем** по описанию пользователя
- 💡 **Подбор услуг** на основе симптомов
- 📋 **Объяснение решений** простым языком
- 🛡️ **Резервная логика** при недоступности ИИ

### Примеры работы:
```
Пользователь: "Компьютер стал медленно работать и шумит"
ИИ: "Проблемы с производительностью обычно связаны с пылью и перегревом. 
     Рекомендую: диагностику, чистку от пыли, замену термопасты."
```

## 🔧 Разработка

### Запуск в режиме разработки:

```bash
# Установка дополнительных инструментов
pip install black isort flake8 pytest

# Форматирование кода
black app/
isort app/

# Проверка стиля
flake8 app/

# Запуск тестов
pytest tests/
```

### Добавление новых функций:

1. **Новый обработчик** - создайте файл в `app/handlers/`
2. **Новые клавиатуры** - добавьте в `app/keyboards/`
3. **Новые константы** - обновите `app/utils/constants.py`
4. **Новые таблицы БД** - модифицируйте `app/database/connection.py`

### Структура обработчика:
```python
from aiogram import Router, F
from aiogram.types import Message, CallbackQuery

# Создаем роутер
my_router = Router()

@my_router.message(F.text == "Моя кнопка")
async def my_handler(message: Message, db_queries, user):
    # Ваш код здесь
    pass

def setup_my_router(db_queries) -> Router:
    return my_router
```

## 📊 Мониторинг

### Логирование:
- Все операции логируются в файл `bot.log`
- Уровень логирования настраивается в `config.txt`
- Ошибки автоматически сохраняются с полной трассировкой

### Статистика:
```python
# Получение статистики через базу данных
stats = await db_queries.get_statistics()
print(f"Пользователей: {stats['total_users']}")
print(f"Заказов: {stats['total_orders']}")
print(f"Средний рейтинг: {stats['average_rating']}")
```

## 🚨 Устранение неполадок

### Частые проблемы:

1. **"Некорректный токен бота"**
   - Проверьте правильность токена в `config.txt`
   - Убедитесь, что файл `config.txt` в правильной кодировке (UTF-8)

2. **"ИИ сервис недоступен"**
   - Проверьте API ключ Gemini
   - Убедитесь в наличии интернет-соединения
   - Бот продолжит работать с резервной логикой

3. **"Ошибка базы данных"**
   - Удалите файл `repair_bot.db` для пересоздания
   - Проверьте права доступа к папке

4. **"Превышен лимит сообщений"**
   - Увеличьте `RATE_LIMIT_MESSAGES` в конфигурации
   - Или увеличьте `RATE_LIMIT_WINDOW`

### Отладка:
```bash
# Запуск с детальным логированием
LOG_LEVEL=DEBUG python -m app.main

# Проверка конфигурации
python -c "from app.config import ConfigLoader; print(ConfigLoader.load_from_file())"
```

## 📝 Лицензия

Этот проект распространяется под MIT лицензией. См. файл LICENSE для подробностей.

## 🤝 Поддержка

Если у вас есть вопросы или предложения:

1. 🐛 **Баги** - создайте Issue в репозитории
2. 💡 **Предложения** - откройте Discussion
3. 📧 **Другие вопросы** - напишите на email

## 🎯 Планы развития

- [ ] **Веб-интерфейс** для администраторов
- [ ] **Система платежей** через Telegram
- [ ] **Уведомления** SMS/Email
- [ ] **Геолокация** для выбора ближайшего мастера
- [ ] **API** для интеграции с внешними системами
- [ ] **Многоязычность** интерфейса
- [ ] **Статистика** и аналитика использования

---

**🚀 Готов к продакшену!** Бот протестирован и готов к использованию в реальных условиях.